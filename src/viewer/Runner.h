#pragma once

#include "viewer/Server.h"

// Convenience umbrella for the user entry file: pulls in startServer(). Kept as its own header so the entry file's
// includes read as intent ("the viewer runtime") rather than an implementation detail.
